# qr-generate

Firebase is now initialized in `src/firebase.js` and exports:

- `app`: initialized Firebase app instance
- `db`: Firestore database instance

## Usage

```js
import { db } from "./src/firebase";
```
